# Manual Test Instructions

- Open the web application using the embedded browser inside VS Code.
- Manually verify the requested changes in the running application.
- After testing is complete, close the embedded browser tab to free resources.
- If you create any temporary resources during testing (for example, database test records or local test files), remove them so the development environment stays clean.

> This instruction file is intended to guide the agent to perform manual verification and cleanup after changes.